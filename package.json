{
  "name": "@xiangnanscu/tiku",
  "version": "0.0.0",
  "scripts": {
    "nginx": "nginx -p . -c nginx.conf",
    "nginx:reload": "yarn nginx -s reload",
    "nginx:stop": "yarn nginx -s stop",
    "push": "git add . && git commit -am 'push' && git push origin master",
    "deploy": "rm -rf dist && ./deploy.sh",
    "deploys": "USE_HTTPS= yarn deploy",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "docx-templates": "^4.9.2",
    "file-saver": "^2.0.5",
    "vue": "^3.2.25",
    "xlsx": "^0.14.2",
    "xlsx-template-compact": "^1.3.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.3",
    "sass": "^1.52.1",
    "vite": "^2.9.9"
  }
}
